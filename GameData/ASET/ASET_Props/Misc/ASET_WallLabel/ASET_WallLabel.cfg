PROP
{
	name = ASET_WallLabel

	MODEL
	{
		model = ASET/ASET_Props/Misc/ASET_WallLabel/ASET_WallLabel
	}

	MODULE
	{
		name = JSISwitchableVariableLabel
		labelTransform = text
		fontSize = 0.020
		alignment = Center

		switchSound = ASET/ASET_Props/Sounds/buttonbeep
		switchTransform = buttonUp
		decrementSwitchTransform = buttonDown
		persistActiveLabel = true

		VARIABLESET
		{
			labelText = 
		}

		VARIABLESET
		{
			labelText = AIRLOCK
		}

		VARIABLESET
		{
			labelText = POD
		}

		VARIABLESET
		{
			labelText = BRIDGE
		}

		VARIABLESET
		{
			labelText = CONTROL
		}

		VARIABLESET
		{
			labelText = HELM
		}

		VARIABLESET
		{
			labelText = ENGINES
		}

		VARIABLESET
		{
			labelText = GALLEY
		}

		VARIABLESET
		{
			labelText = HAB
		}

		VARIABLESET
		{
			labelText = CREW
		}

		VARIABLESET
		{
			labelText = MEDICAL
		}

		VARIABLESET
		{
			labelText = BRIG
		}

		VARIABLESET
		{
			labelText = ENGINEERING
		}

		VARIABLESET
		{
			labelText = ESCAPE
		}

		VARIABLESET
		{
			labelText = STORAGE
		}

		VARIABLESET
		{
			labelText = SCI LAB
		}

		VARIABLESET
		{
			labelText = DOCKING
		}

		VARIABLESET
		{
			labelText = PORT
		}

		VARIABLESET
		{
			labelText = STARBOARD
		}

		VARIABLESET
		{
			labelText = FORE
		}

		VARIABLESET
		{
			labelText = AFT
		}

		VARIABLESET
		{
			labelText = ZENITH
		}

		VARIABLESET
		{
			labelText = NADIR
		}
	}

	MODULE:NEEDS[KerbalVR]
	{
		name = VRButton
		buttonTransformName = buttonUp
	}

	MODULE:NEEDS[KerbalVR]
	{
		name = VRButton
		buttonTransformName = buttonDown
	}

	MODULE:NEEDS[FreeIva]
	{
		name = PhysicalProp
		isSticky = true
        grabSound = FreeIva/Sounds/velcro4
        stickSound = FreeIva/Sounds/velcro4
	}
}